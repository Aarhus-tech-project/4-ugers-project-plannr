import { defaultSystem } from "@chakra-ui/react"

/**
 * Use defaultSystem for Chakra UI v3 theming.
 * Customization is currently limited; check the official docs for updates.
 */
export const theme = defaultSystem
